// Database file - no longer needed for static Netlify deployment
// This app is now fully client-side using Netlify Forms

// Placeholder exports in case they're referenced elsewhere
export const pool = null;
export const db = null;
